{
  "steam_api_key": {
    "description": "Steam Web API 密钥",
    "type": "string",
    "default": "",
    "hint": "请在 https://steamcommunity.com/dev/apikey 获取"
  },
  "admin_only_sensitive_operations": {
    "description": "敏感操作需管理员权限",
    "type": "bool",
    "default": true,
    "hint": "开启后，'steam alllist', 'steam add', 'steam remove', 'steam health' 命令将仅限机器人管理员使用。"
  },
  "status_poll_interval": {
    "description": "检查【在线/游戏状态】的间隔（秒）",
    "type": "int",
    "default": 180
  },
  "achievement_poll_interval": {
    "description": "检查【成就状态】的间隔（秒），设为 0 则关闭成就监控",
    "type": "int",
    "default": 1800
  },
  "retry_times": {
    "description": "请求失败时的最大重试次数",
    "type": "int",
    "default": 3
  },
  "detailed_poll_log": {
    "description": "是否在控制台输出详细轮询日志",
    "type": "bool",
    "default": false
  },
  "log_masking": {
    "description": "日志脱敏",
    "type": "bool",
    "default": true,
    "hint": "开启后，日志中的 Steam API Key 和 17 位 Steam ID 会显示为 ***。"
  },
  "status_notification": {
    "description": "【总开关】是否启用Steam状态通知（游戏、在线/离线）",
    "type": "bool",
    "default": true
  },
  "online_offline_notification": {
    "description": "是否启用上线/下线提醒（仅当“状态通知”开启时生效）",
    "type": "bool",
    "default": false
  },
  "achievements_notification": {
    "description": "【总开关】是否启用成就通知",
    "type": "bool",
    "default": true
  },
  "playtime_notification": {
    "description": "【总开关】是否启用游戏时长通知",
    "type": "bool",
    "default": true
  },
  "private_mode": {
    "description": "【总开关】是否启用隐私模式",
    "type": "bool",
    "default": false,
    "hint": "若开启，则插件主动推送的所有消息，其中的【Steam昵称】字段全部变为隐私代称。"
  },
  "private_name": {
    "description": "隐私代称",
    "type": "string",
    "default": "",
    "hint": "若为空则推送消息的主语默认变为“有人”。例如：【Steam昵称】上线了→有人上线了；若输入“神秘群友”：【Steam昵称】上线了→神秘群友上线了。"
  },
  "monitored_targets": {
    "description": "监控目标",
    "type": "template_list",
    "hint": "点击【添加条目】创建监控组。每个监控组可绑定多个会话和多个 Steam 用户，并可单独覆盖全局推送设置。",
    "templates": {
      "monitor_group": {
        "name": "监控组",
        "hint": "一个监控组将指定的 Steam 用户状态推送到指定的会话中",
        "items": {
          "note": {
            "description": "备注",
            "type": "string",
            "default": "",
            "hint": "可选，方便辨别不同监控组（如宿舍群、工作群）"
          },
          "sessions": {
            "description": "推送会话列表",
            "type": "list",
            "default": [],
            "hint": "填写完整的会话ID（如 napcat:GroupMessage:987654321），可在对话中发送 sid 命令查看当前会话ID"
          },
          "steam_ids": {
            "description": "监控的 Steam ID 列表",
            "type": "list",
            "default": [],
            "hint": "填写17位 Steam ID。首次保存后会自动补上昵称备注，例如 76561198xxxxxxxxx（张三）。改成 ID（） 会重新自动获取；删除（）表示关闭该用户的备注。"
          },
          "settings": {
            "description": "推送设置",
            "type": "object",
            "hint": "每项都可选择：跟随全局、开启或关闭",
            "items": {
              "status_notification_mode": {
                "description": "状态通知",
                "type": "string",
                "options": ["inherit", "on", "off"],
                "labels": ["跟随全局", "开启", "关闭"],
                "default": "inherit",
                "hint": "控制游戏开始/结束通知"
              },
              "online_offline_notification_mode": {
                "description": "上下线通知",
                "type": "string",
                "options": ["inherit", "on", "off"],
                "labels": ["跟随全局", "开启", "关闭"],
                "default": "inherit",
                "hint": "控制上线/下线提醒，仍依赖状态通知开启"
              },
              "achievements_notification_mode": {
                "description": "成就通知",
                "type": "string",
                "options": ["inherit", "on", "off"],
                "labels": ["跟随全局", "开启", "关闭"],
                "default": "inherit",
                "hint": "控制新成就通知"
              },
              "playtime_notification_mode": {
                "description": "游戏时长通知",
                "type": "string",
                "options": ["inherit", "on", "off"],
                "labels": ["跟随全局", "开启", "关闭"],
                "default": "inherit",
                "hint": "控制游戏时长变化通知"
              },
              "privacy_mode_setting": {
                "description": "隐私模式",
                "type": "string",
                "options": ["inherit", "on", "off"],
                "labels": ["跟随全局", "开启", "关闭"],
                "default": "inherit",
                "hint": "开启后用隐私代称代替Steam昵称"
              }
            }
          }
        }
      }
    }
  }
}
